# 📊 Placements System - Полное руководство

## 🎯 Обзор системы

**Placements (Размещения)** - основная система для управления рекламными размещениями между рекламодателями и блогерами в AdMarket.

---

## 📋 Lifecycle размещения

```
proposal → booked → in_progress → posted → approved/rejected
   ↓          ↓           ↓          ↓           ↓
Предложение Принято   В работе  Опубл-но   Завершено
(блогеру)  (блогером) (блогер)  (блогер)  (рекламод.)
```

### Статусы размещений:

| Статус | Описание | Кто может изменить |
|--------|----------|-------------------|
| `proposal` | Предложение отправлено блогеру | Блогер (принять/отклонить) |
| `booked` | Блогер принял предложение | Блогер (начать работу) |
| `in_progress` | Блогер работает над контентом | Блогер (опубликовать) |
| `posted` | Контент опубликован, ждёт проверки | Рекламодатель (одобрить/отклонить) |
| `approved` | Размещение одобрено и завершено | Система |
| `rejected` | Размещение отклонено | Система |

---

## 👥 Интерфейсы пользователей

### 🏢 Рекламодатель

#### 1. Создание размещений
**Путь:** `/campaigns/[id]` → Выбрать блогеров → "Создать размещения"

**Что происходит:**
- Массовое создание размещений для выбранных каналов
- Автосоздание форматов, если их нет у канала
- Отправка уведомлений всем блогерам
- Редирект на страницу размещений кампании

**API:** `POST /api/placements`
```typescript
{
  campaign_id: string,
  channel_ids: string[]
}
```

#### 2. Просмотр размещений кампании
**Путь:** `/campaigns/[id]/placements`

**Функции:**
- Список всех размещений кампании
- Фильтрация по статусам
- Визуализация прогресса
- Метрики каналов (подписчики, просмотры, ER)
- Дедлайны и стоимость

**API:** `GET /api/placements?campaign_id={id}`

#### 3. Одобрение/отклонение контента
**Путь:** `/campaigns/[id]/placements` → Карточка размещения

**Доступно для:** Размещения со статусом `posted`

**API:** `PATCH /api/placements/[id]`
```typescript
{
  status: 'approved' | 'rejected',
  rejection_reason?: string
}
```

---

### 🎥 Блогер (Creator)

#### 1. Просмотр предложений
**Путь:** `/creator/proposals`

**Функции:**
- Список размещений со статусом `proposal`
- Детали кампании и бренда
- Информация о формате и оплате
- Дедлайны
- Действия: Принять / Отклонить

**Фильтрация:**
- Автоматически показываются только предложения для каналов блогера
- Query: `GET /api/placements?channel_ids={ids}&status=proposal`

**Действия:**
```typescript
// Принять предложение
PATCH /api/placements/[id]
{ status: 'booked' }

// Отклонить предложение
PATCH /api/placements/[id]
{ status: 'rejected' }
```

#### 2. Управление активными размещениями
**Путь:** `/creator/placements`

**Функции:**
- Статистика по статусам (в работе, забронировано, опубликовано, одобрено)
- Фильтрация по статусам
- Индикаторы дедлайнов (просрочен, сегодня, завтра, X дней)
- Прогресс-трекинг

**Статусы для блогера:**
- `booked` → Кнопка "Начать работу"
- `in_progress` → Кнопка "Отметить как опубликовано"
- `posted` → Ожидание одобрения рекламодателя
- `approved` → Размещение завершено ✅

**API:** `GET /api/placements?channel_ids={ids}&status=in.(booked,in_progress,posted,approved)`

#### 3. Публикация контента
**Модальное окно:** При нажатии "Отметить как опубликовано"

**Поля:**
- Ссылка на публикацию (required, URL validation)
- Превью кампании и формата

**API:** `PATCH /api/placements/[id]`
```typescript
{
  status: 'posted',
  post_link: string
}
```

**Результат:**
- Создаётся уведомление рекламодателю типа `content_uploaded`
- Ссылка сохраняется в базе данных
- Optimistic UI - размещение исчезает из списка "В работе"

---

## 🔔 Система уведомлений

### Автоматические уведомления:

| Событие | Получатель | Тип | Сообщение |
|---------|-----------|------|-----------|
| Создание размещения | Блогер | `new_placement_request` | "Вам поступило предложение размещения..." |
| Принятие предложения | Рекламодатель | `placement_accepted` | "Блогер принял предложение..." |
| Отклонение предложения | Рекламодатель | `placement_rejected` | "Блогер отклонил предложение..." |
| Публикация контента | Рекламодатель | `content_uploaded` | "Блогер опубликовал контент..." |
| Одобрение размещения | Блогер | `placement_approved` | "Размещение одобрено..." |
| Отклонение размещения | Блогер | `placement_rejected` | "Размещение отклонено..." |

**Реализация:**
- Создаются в API endpoints автоматически
- Хранятся в таблице `notifications`
- Поля: `user_id`, `type`, `title`, `message`, `campaign_id`, `channel_id`, `is_read`

---

## 📡 API Endpoints

### Placements

#### `GET /api/placements`
**Параметры:**
- `campaign_id` - фильтр по кампании
- `channel_ids` - фильтр по каналам (comma-separated)
- `status` - фильтр по статусу
  - Одиночный: `status=proposal`
  - Множественный: `status=in.(booked,in_progress,posted)`

**Ответ:**
```typescript
{
  success: true,
  placements: Placement[]
}
```

#### `POST /api/placements`
**Тело запроса:**
```typescript
{
  campaign_id: string,
  channel_ids: string[]
}
```

**Логика:**
- Получить кампанию и каналы
- Для каждого канала:
  - Получить или создать формат (default: post, 10000 RUB, 3 days SLA)
  - Создать placement со статусом `proposal`
  - Рассчитать deadline = now + format.sla_days
- Bulk insert placements
- Создать уведомления для всех блогеров
- Bulk insert notifications

**Ответ:**
```typescript
{
  success: true,
  placements: Placement[],
  notifications: Notification[]
}
```

#### `GET /api/placements/[id]`
**Ответ:**
```typescript
{
  success: true,
  placement: Placement
}
```

#### `PATCH /api/placements/[id]`
**Тело запроса:**
```typescript
{
  status: PlacementStatus,
  post_link?: string,
  rejection_reason?: string
}
```

**Логика:**
- Обновить placement
- Создать соответствующее уведомление
- Вернуть обновлённый placement

---

## 🗄️ Структура данных

### Placement

```typescript
interface Placement {
  id: string
  campaign_id: string
  channel_id: string
  format_id: string
  
  // Pricing
  unit_price: {
    value: number
    currency: string
    model: 'fixed' | 'cpm' | 'cpc'
  }
  
  // Timeline
  deadline_at: string  // ISO 8601
  
  // Status
  status: 'proposal' | 'booked' | 'in_progress' | 'posted' | 'approved' | 'rejected'
  
  // Content
  post_link?: string
  assets: string[]  // URLs to uploaded assets
  
  // Metadata
  created_at: string
  updated_at: string
  
  // Relations
  campaign: Campaign
  channel: Channel
  format: Format
}
```

### Format (автосоздаётся при отсутствии)

```typescript
const defaultFormat = {
  channel_id: string,
  name: 'post',
  rights: 'standard',
  price: {
    value: 10000,
    currency: 'RUB',
    model: 'fixed'
  },
  sla_days: 3,
}
```

---

## 🎨 UI Компоненты

### Status Badges

```typescript
const STATUS_LABELS = {
  proposal: {
    label: 'Предложение',
    icon: Clock,
    variant: 'secondary',
    color: 'text-gray-600',
    bgColor: 'bg-gray-50'
  },
  booked: {
    label: 'Забронировано',
    icon: Calendar,
    variant: 'default',
    color: 'text-blue-600',
    bgColor: 'bg-blue-50'
  },
  in_progress: {
    label: 'В работе',
    icon: Loader2,
    variant: 'default',
    color: 'text-orange-600',
    bgColor: 'bg-orange-50'
  },
  posted: {
    label: 'Опубликовано',
    icon: CheckCircle,
    variant: 'success',
    color: 'text-green-600',
    bgColor: 'bg-green-50'
  },
  approved: {
    label: 'Одобрено',
    icon: CheckCircle2,
    variant: 'success',
    color: 'text-emerald-600',
    bgColor: 'bg-emerald-50'
  },
  rejected: {
    label: 'Отклонено',
    icon: XCircle,
    variant: 'destructive',
    color: 'text-red-600',
    bgColor: 'bg-red-50'
  }
}
```

### Deadline Status Indicators

```typescript
const getDeadlineStatus = (deadlineString: string) => {
  const daysRemaining = getDaysRemaining(deadlineString)
  
  if (daysRemaining < 0) {
    return { text: 'Просрочен', color: 'text-red-600', bgColor: 'bg-red-50' }
  } else if (daysRemaining === 0) {
    return { text: 'Сегодня', color: 'text-orange-600', bgColor: 'bg-orange-50' }
  } else if (daysRemaining === 1) {
    return { text: 'Завтра', color: 'text-orange-600', bgColor: 'bg-orange-50' }
  } else if (daysRemaining <= 3) {
    return { text: `${daysRemaining} дня`, color: 'text-yellow-600', bgColor: 'bg-yellow-50' }
  } else {
    return { text: `${daysRemaining} дней`, color: 'text-gray-600', bgColor: 'bg-gray-50' }
  }
}
```

---

## 🚀 Workflow примеры

### Сценарий 1: Успешное размещение

1. **Рекламодатель создаёт кампанию** → заходит в `/campaigns/[id]`
2. **Выбирает 5 блогеров** → нажимает "Создать размещения (5)"
3. **Система создаёт 5 placements** → отправляет 5 уведомлений
4. **Блогер А получает уведомление** → заходит в `/creator/proposals`
5. **Видит предложение** → нажимает "Принять" → статус: `booked`
6. **Рекламодатель получает уведомление** → "Блогер А принял предложение"
7. **Блогер А нажимает "Начать работу"** → статус: `in_progress`
8. **Создаёт контент** → публикует → нажимает "Отметить как опубликовано"
9. **Вводит ссылку на пост** → https://youtube.com/watch?v=xxx
10. **Рекламодатель получает уведомление** → проверяет контент
11. **Одобряет размещение** → статус: `approved` ✅
12. **Блогер получает уведомление** → "Размещение одобрено"

### Сценарий 2: Отклонение предложения

1. **Рекламодатель создаёт размещение** → блогер получает уведомление
2. **Блогер открывает `/creator/proposals`** → видит предложение
3. **Нажимает "Отклонить"** → статус: `rejected`
4. **Рекламодатель получает уведомление** → "Блогер отклонил предложение"
5. **Может выбрать другого блогера** → создать новое размещение

### Сценарий 3: Просрочка дедлайна

1. **Placement создан с deadline = now + 3 days**
2. **Блогер принимает** → статус: `booked`
3. **Проходит 3 дня** → deadline_at < now
4. **В `/creator/placements`** → индикатор "Просрочен" (красный)
5. **Блогер всё равно может опубликовать** → система не блокирует
6. **Рекламодатель видит просрочку** → может обсудить в Direct Messages

---

## 📊 Метрики и аналитика

### Для рекламодателя (на странице кампании)

```typescript
// Статистика размещений
const stats = {
  total: placements.length,
  proposal: placements.filter(p => p.status === 'proposal').length,
  booked: placements.filter(p => p.status === 'booked').length,
  in_progress: placements.filter(p => p.status === 'in_progress').length,
  posted: placements.filter(p => p.status === 'posted').length,
  approved: placements.filter(p => p.status === 'approved').length,
  rejected: placements.filter(p => p.status === 'rejected').length,
}

// Конверсия
const conversionRate = (stats.approved / stats.total) * 100

// Средняя стоимость
const avgPrice = placements.reduce((sum, p) => sum + p.unit_price.value, 0) / placements.length
```

### Для блогера (на странице размещений)

```typescript
// Активные размещения
const active = {
  booked: placements.filter(p => p.status === 'booked').length,
  in_progress: placements.filter(p => p.status === 'in_progress').length,
  posted: placements.filter(p => p.status === 'posted').length,
  approved: placements.filter(p => p.status === 'approved').length,
}

// Заработок
const totalEarnings = placements
  .filter(p => p.status === 'approved')
  .reduce((sum, p) => sum + p.unit_price.value, 0)

// Средний срок выполнения
const avgCompletionTime = calculateAvgTime(placements.filter(p => p.status === 'approved'))
```

---

## 🔧 Troubleshooting

### Проблема: Размещения не создаются

**Решение:**
1. Проверить, что кампания существует
2. Проверить, что каналы существуют и активны
3. Проверить наличие owner_user_id у каналов
4. Проверить логи API: `POST /api/placements`

### Проблема: Уведомления не приходят

**Решение:**
1. Проверить таблицу `notifications` в Supabase
2. Проверить, что notifications создаются в API endpoints
3. Проверить user_id в уведомлениях совпадает с текущим пользователем
4. Проверить фильтр `is_read=false`

### Проблема: Статус не обновляется

**Решение:**
1. Проверить права доступа к `PATCH /api/placements/[id]`
2. Проверить валидацию статусных переходов
3. Проверить, что новый статус корректный
4. Проверить network tab в DevTools

---

## 🎯 Best Practices

### Для рекламодателей

1. **Чёткое описание кампании** - помогает блогерам понять требования
2. **Реалистичные дедлайны** - учитывать время на создание контента
3. **Быстрая проверка контента** - блогеры ждут одобрения
4. **Конструктивная обратная связь** - при отклонении указывать причину

### Для блогеров

1. **Быстрое реагирование** - принимать/отклонять предложения оперативно
2. **Соблюдение дедлайнов** - планировать время на создание контента
3. **Качественный контент** - соответствие требованиям кампании
4. **Корректные ссылки** - проверять ссылки на публикации перед отправкой

### Для разработчиков

1. **Используйте bulk operations** - для создания множества размещений
2. **Создавайте уведомления асинхронно** - не блокируйте основной flow
3. **Валидируйте переходы статусов** - только допустимые переходы
4. **Логируйте критические операции** - для debugging
5. **Используйте transactions** - для атомарных операций

---

## 📝 TODO / Roadmap

### В разработке

- [ ] **Комментарии к размещениям** - обсуждение деталей
- [ ] **Ревизии контента** - запрос изменений от рекламодателя
- [ ] **Файловые вложения** - загрузка assets перед публикацией
- [ ] **Аналитика размещений** - детальная статистика
- [ ] **Шаблоны форматов** - предустановленные форматы для быстрого создания
- [ ] **Bulk actions** - массовое одобрение/отклонение

### Планируется

- [ ] **Автоматическое продление дедлайнов** - если блогер запрашивает
- [ ] **Эскалация просрочек** - уведомления при просрочке
- [ ] **Rating system** - оценка выполнения размещений
- [ ] **Dispute resolution** - система разрешения споров
- [ ] **Contract generation** - автоматическое создание договоров
- [ ] **Payment integration** - интеграция с платёжными системами

---

## 🔗 Связанные модули

- **Campaigns** - создание и управление кампаниями
- **Channels** - каналы блогеров
- **Formats** - форматы размещений
- **Notifications** - система уведомлений
- **Direct Messages** - общение между рекламодателем и блогером

---

## 📞 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте раздел Troubleshooting
2. Проверьте логи API endpoints
3. Проверьте таблицы в Supabase
4. Создайте issue в GitHub репозитории

---

**Версия документации:** 1.0  
**Последнее обновление:** 2025-11-25  
**Статус:** ✅ Production Ready
